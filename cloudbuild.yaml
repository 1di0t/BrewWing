# Cloud Build setting file
# for Google Cloud Build
options:
  logging: CLOUD_LOGGING_ONLY

# Add a substitution variable to access the secret directly
substitutions:
  _HF_API_KEY: 'hf_dummy_token_for_testing_purposes_only_replace_in_production'
  _DJANGO_SECRET_KEY: 'django_dummy_secret_key_for_testing_purposes_only'

# Access secrets directly
availableSecrets:
  secretManager:
    - versionName: projects/$PROJECT_ID/secrets/brew-wing-secret/versions/latest
      env: 'HF_API_KEY'
    - versionName: projects/$PROJECT_ID/secrets/django-secret/versions/latest
      env: 'DJANGO_SECRET_KEY'

steps:
  # backend build and push
  #=========================================================================
  - name: 'gcr.io/cloud-builders/docker'
    id: 'build-backend'
    secretEnv: ['HF_API_KEY', 'DJANGO_SECRET_KEY']
    args:
      - "build"
      - "-t"
      - "gcr.io/$PROJECT_ID/brewwing-backend:latest"
      - "-f"
      - "Dockerfile.backend"
      - "--build-arg"
      - "HF_API_KEY=${_HF_API_KEY}"
      - "--build-arg"
      - "DJANGO_SECRET_KEY=${_DJANGO_SECRET_KEY}"
      - "--no-cache"
      - "."
  - name: 'gcr.io/cloud-builders/docker'
    args:
      [
        'push',
        'gcr.io/$PROJECT_ID/brewwing-backend:latest'
      ]
  # frontend build and push
  #=========================================================================
  - name: 'gcr.io/cloud-builders/docker'
    args:
      [
        'build',
        '-t', 'gcr.io/brewwing/brewwing-frontend:latest',
        '-f','brewing/react-app/Dockerfile.frontend',
        '.'
      ]
  - name: 'gcr.io/cloud-builders/docker'
    args:
      [
        'push',
        'gcr.io/brewwing/brewwing-frontend:latest'
      ]
  # backend deploy
  #=========================================================================
  - name: 'gcr.io/google.com/cloudsdktool/cloud-sdk'
    entrypoint: 'gcloud'
    args:
      [
        'run', 'deploy', 'brewwing-backend',
        '--image', 'gcr.io/$PROJECT_ID/brewwing-backend:latest',
        '--platform', 'managed',
        '--region', 'us-central1',
        '--allow-unauthenticated',
      ]
  # frontend deploy
  #=========================================================================
  - name: 'gcr.io/google.com/cloudsdktool/cloud-sdk'
    entrypoint: 'gcloud'
    args:
      [
        'run', 'deploy', 'brewwing-frontend',
        '--image', 'gcr.io/$PROJECT_ID/brewwing-frontend:latest',
        '--platform', 'managed',
        '--region', 'us-central1',
        '--allow-unauthenticated'
      ]

timeout: 2400s 
